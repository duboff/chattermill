# For more information see: http://emberjs.com/guides/routing/
App.Router.reopen
  location: 'auto'
  rootURL: '/'

App.Router.map ()->
  # @resource('posts')

  # @resource 'posts', path: '/posts', ->
  #   @route 'new', path: '/new'

